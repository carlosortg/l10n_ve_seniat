<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_withholding_list" model="ir.ui.view">
        <field name="name">account.withholding.list</field>
        <field name="model">account.withholding</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="date"/>
                <field name="partner_id"/>
                <field name="withholding_type"/>
                <field name="amount_withheld"/>
                <field name="state"/>
            </list>
        </field>
    </record>
    <record id="view_account_withholding_form" model="ir.ui.view">
        <field name="name">account.withholding.form</field>
        <field name="model">account.withholding</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" string="Confirmar" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_declare" string="Marcar declarado" type="object" invisible="state != 'confirmed'"/>
                    <button name="action_cancel" string="Anular" type="object" invisible="state not in ('draft','confirmed')"/>
                    <button name="action_draft" string="Volver a borrador" type="object" invisible="state != 'cancelled'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,declared"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="date"/>
                            <field name="partner_id"/>
                            <field name="withholding_type"/>
                        </group>
                        <group>
                            <field name="amount_base"/>
                            <field name="rate"/>
                            <field name="amount_withheld"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                    <field name="invoice_ids" widget="many2many_tags"/>
                    <field name="line_ids">
                        <list editable="bottom">
                            <field name="invoice_id"/>
                            <field name="amount_base"/>
                            <field name="rate"/>
                            <field name="amount_withheld"/>
                        </list>
                    </field>
                    <field name="notes"/>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    <record id="action_account_withholding" model="ir.actions.act_window">
        <field name="name">Comprobantes de retencion</field>
        <field name="res_model">account.withholding</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
